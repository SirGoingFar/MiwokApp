<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eemf.miwokapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".NumbersActivity"
            android:label="@string/listNumberHeader"
            android:parentActivityName=".MainActivity"/>

        <activity
            android:name=".FamilyMembersActivity"
            android:label="@string/listFamilyMemberHeader"
            android:parentActivityName=".MainActivity"/>

        <activity
            android:name=".ColorsActivity"
            android:label="@string/listColorHeader"
            android:parentActivityName=".MainActivity"/>

        <activity
            android:name=".PhrasesActivity"
            android:label="@string/listPhraseHeader"
            android:parentActivityName=".MainActivity"/>


    </application>

</manifest>